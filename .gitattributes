# Normalize every text file to LF in the repository, whatever the checkout does
# locally. Without this, a Windows checkout commits CRLF and every later diff on
# a Linux runner (CI) reads as a whole-file rewrite.
* text=auto eol=lf
*.png binary
*.jpg binary
*.webp binary
