"""Node classes, generated from a ``GenrePack``.

**Nothing in this package may name a genre.** Both modules here export a
factory that takes a pack and returns a ``io.ComfyNode`` subclass; the
repo-root ``__init__.py`` supplies the pack. ``tests/test_genre_contract.py``
greps this directory for genre slugs and fails if one appears.
"""
